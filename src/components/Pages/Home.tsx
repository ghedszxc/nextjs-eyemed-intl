interface IHome {
  children: React.ReactNode;
}

const Home: React.FC<IHome> = ({ children }) => {
  // Hooks
  // Variables
  // Functions
  // Effects

  return <div>{children}</div>;
};
export default Home;
